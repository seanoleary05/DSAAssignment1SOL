package models;

import org.w3c.dom.Node;

public class LinkedList {
    public Node head;
    public class Node {


    public Node next;
    Object Show;
}

    public Node addNewNode(){
    Node temp=head;

    while(temp!=null) {
        temp = temp.next;
    }
    return null;
    }
}
